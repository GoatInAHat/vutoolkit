# Generated by toolfactory. Do not edit; run `toolfactory build` to regenerate.

services:
  openclaw:
    image: ghcr.io/openclaw/openclaw:latest
    volumes:
      - .:/work
      - PLUGIN_DATA:/data
    env_file:
      - .env
    command:
      - sh
      - -c
      - openclaw plugins install --link /work/hosts/openclaw --force --accept-capabilities && openclaw plugins inspect vutoolkit --runtime --json
volumes:
  PLUGIN_DATA: {}
